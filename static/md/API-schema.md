You can use `Edge-Connector` without `KIT GUI`, for your application system to interact with.

## API endpoints

Can be found in `localhost:8001/docs`.

## Schemas

It can also be found in `localhost:8001/docs`.
For the Basic KIT schema, 